Registration API

Example registration request (JSON):

```json
{
  "firstName": "Jane",
  "lastName": "Doe",
  "email": "jane.doe@example.com",
  "password": "Password1",
  "confirmPassword": "Password1",
  "age": 30
}
```

Use the GET /api/registration endpoint to retrieve this sample from the running API.
